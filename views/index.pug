include include/sauce-common

doctype
html(lang='en')
  head
    include include/head.pug
    title SAUCE
  body
    +sauce-container('index')(pageId='index', user=user)
      if user
        include dashboard
      else
        include homepage-splash
    script(type='text/javascript', src='/dist/js/bootstrapScripts.js')
